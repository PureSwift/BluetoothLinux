/*
 * Placeholder translation unit.
 *
 * CMake requires a shared library target to have at least one source of
 * its own; every exported symbol actually arrives from the Swift and C
 * static archives, linked whole (see CMakeLists.txt).
 */

/* Deliberately empty. */
